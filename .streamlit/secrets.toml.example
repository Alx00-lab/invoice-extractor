# Copy this file to `.streamlit/secrets.toml` and fill in your real key.
# `secrets.toml` is gitignored — never edit this template in place with a real key.
#
# For Streamlit Cloud, paste the same key into the app's "Secrets" UI instead
# of deploying secrets.toml.

OPENAI_API_KEY = "sk-REPLACE_ME"
